# CLI Reference

The following reference is generated from the ZUtils Typer app. Global options (`--file-logging`, `--log-file`) must appear **before** the subcommand.

::: mkdocs-typer2
    :module: zutils.cli.main
    :name: zutils
