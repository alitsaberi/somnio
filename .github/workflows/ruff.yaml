name: Ruff
on: [ pull_request ]
jobs:
  ruff:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # Pin to the project/dev Ruff so CI does not float to a newer defaults set.
      - uses: astral-sh/ruff-action@v3
        with:
          version: "0.14.14"
